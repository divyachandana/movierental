
<%= link_to 'New Rental', new_rental_path %> 
<table align="left">
  <tr>
  	<th>language</th>
    <th>moviename</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 
  <% @rentals.each do |rental| %>
    <tr>
    	<td><%= rental.mobileno %></td>
      <td><%= rental.moviename %></td>
    <td><%= link_to 'Delete', rental_path(rental),
                    method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<table align="right">
	<tr><td><%= link_to "HOME" , '/welcome/index.html'%></td></tr>
	<tr><td><%= link_to "Movies", controller: "movies" %></td></tr>
	<tr><td><%= link_to 'New movie', new_movie_path %> </td></tr>
	<tr><td><%= link_to "Customers", controller: "customers" %></td></tr>
	<tr><td><%= link_to 'New customer', new_customer_path %></td><tr>
	<tr><td><%= link_to 'New rental', new_rental_path %></td></tr>

</table>